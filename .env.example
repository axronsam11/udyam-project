# API Configuration (placeholder - not used in demo)
NEXT_PUBLIC_API_BASE_URL="https://api.example.com"

# Database Configuration (placeholder - for future backend implementation)
# This is not used in the current demo as all data is stored locally
MONGODB_URI="mongodb+srv://username:password@cluster.mongodb.net/udyam_db"

# Environment
NODE_ENV="development"

# Security (for future implementation)
NEXTAUTH_SECRET="your-secret-key"
NEXTAUTH_URL="http://localhost:3000"

# Rate Limiting (for future API implementation)
RATE_LIMIT_MAX="100"
RATE_LIMIT_WINDOW_MS="900000"

# File Upload (for future implementation)
MAX_FILE_SIZE="5242880"
ALLOWED_FILE_TYPES=".pdf,.jpg,.jpeg,.png"

# Email Service (for future notifications)
SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="your-email@example.com"
SMTP_PASS="your-password"